<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trade.mapper.CommentMapper">

    <insert id="insert" parameterType="com.campus.trade.entity.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (product_id, user_id, content)
        VALUES (#{productId}, #{userId}, #{content})
    </insert>

    <select id="selectByProduct" resultType="com.campus.trade.entity.Comment">
        SELECT c.*, u.nickname AS user_name, u.avatar AS user_avatar
        FROM comment c
                 JOIN `user` u ON u.id = c.user_id
        WHERE c.product_id = #{productId}
        ORDER BY c.id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByProduct" resultType="long">
        SELECT COUNT(*) FROM comment WHERE product_id = #{productId}
    </select>
</mapper>
